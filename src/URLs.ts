import { ROUTE } from "./constants";

export const URLs = {
  artists: `${ROUTE.app}/${ROUTE.artists}`,
  albums: `${ROUTE.app}/${ROUTE.albums}`,
  songs: `${ROUTE.app}/${ROUTE.songs}`,
  artist: `${ROUTE.app}/${ROUTE.artists}`,
  album: `${ROUTE.app}/${ROUTE.albums}`,
  song: `${ROUTE.app}/${ROUTE.songs}`,
};
